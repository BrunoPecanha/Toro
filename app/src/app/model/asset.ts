export class Asset {
    public id: string;  
    public currentPrince: number;  
 
    
    constructor(id: string, currentPrince: number){
        this.id = id;
        this.currentPrince  = currentPrince;
    }
}